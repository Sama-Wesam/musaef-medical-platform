<?php

return [

    /*
    |--------------------------------------------------------------------------
    | AI Smart Matching Engine Configurations
    |--------------------------------------------------------------------------
    |
    | هنا يتم تحديد الإعدادات الخاصة بخوارزمية المطابقة الذكية بين طلبات الطوارئ
    | والمتبرعين، مثل النطاق الجغرافي الافتراضي والحد الأدنى لنسبة التطابق.
    |
    */

    'matching' => [
        'default_radius_km' => env('AI_MATCHING_RADIUS', 15),
        'min_match_score'   => env('AI_MIN_MATCH_SCORE', 60.0), // الحد الأدنى للقبول
        'max_donors_limit'  => env('AI_MAX_DONORS_LIMIT', 10),
    ],

    /*
    |--------------------------------------------------------------------------
    | Blood Demand & Shortage Forecasting (AI)
    |--------------------------------------------------------------------------
    |
    | الإعدادات الخاصة بنموذج التنبؤ بنقص الدم المستقبلي في المستشفيات
    | بناءً على البيانات التاريخية وسجلات التبرع السابقة.
    |
    */

    'forecast' => [
        'historical_months_required' => env('AI_FORECAST_HISTORY_MONTHS', 6),
        'prediction_days_interval'   => env('AI_FORECAST_DAYS_INTERVAL', 30),
    ],

    /*
    |--------------------------------------------------------------------------
    | Fraud & Spam Detection AI
    |--------------------------------------------------------------------------
    |
    | المعايير الخاصة بمحرك فحص السلوك المشبوه والطلبات الوهمية للمستشفيات
    | لحماية بنوك الدم من التلاعب أو إغراق النظام بطلبات عشوائية.
    |
    */

    'fraud_detection' => [
        'max_allowed_units_per_day' => env('AI_FRAUD_MAX_UNITS', 50),
        'suspicious_score_threshold' => env('AI_FRAUD_THRESHOLD', 50), // حظر إذا تجاوز 50
    ],

];
